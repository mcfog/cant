coco = require \coco
module.exports = class extends require \./module
    ->super ...
    re: '(.+)$'
    evalPath: ({req, res})->
        path = @config \localpath
        path += req.params.0
        path
    evalCdnPath: (path)-> path

    handleData: (data, cb)->cb null, data